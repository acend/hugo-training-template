apiVersion: v1
kind: Service
metadata:
  name: {{ include "acend-hugo-template.fullname" . }}
  labels:
    {{- include "acend-hugo-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acend-hugo-template.selectorLabels" . | nindent 4 }}
