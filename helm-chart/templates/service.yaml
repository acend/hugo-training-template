apiVersion: v1
kind: Service
metadata:
  name: {{ include "acend-training-chart.fullname" . }}
  labels:
    {{- include "acend-training-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acend-training-chart.selectorLabels" . | nindent 4 }}
